/**
 * Supabase Client Configuration
 * This file is prepared for Supabase integration.
 * Currently, the project uses Prisma with MySQL.
 */

// import { createClient } from '@supabase/supabase-js'

// const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL || ''
// const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || ''

// export const supabase = createClient(supabaseUrl, supabaseAnonKey)

export const supabasePlaceholder = {
  message: "Supabase client configuration ready. Please add NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY to your .env file."
};
